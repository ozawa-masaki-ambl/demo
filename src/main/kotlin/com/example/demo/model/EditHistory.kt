package com.example.demo.model

data class EditHistory(var purchaseId: Int?,
                       var customerId: Int?,
                       var productId: Int?,
                       var quantity: Int?)
